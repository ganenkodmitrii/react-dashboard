import logo from '../../resources/logo.png';

const Logo = () => {
  return (
    <div className="box-logo">
      <img className="logo" src={logo} alt="logo" />
    </div>
  );
};
export default Logo;
